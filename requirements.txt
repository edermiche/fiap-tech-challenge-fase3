# Pipeline (bronze, silver, streaming)
pandas>=2.0
pyarrow>=14.0
openpyxl>=3.1
requests>=2.31,<3

# Extração BigQuery (Base dos Dados)
google-cloud-bigquery>=3.14
db-dtypes>=1.2
python-dotenv>=1.0
pydata-google-auth>=1.8

# Validadores visuais das camadas (app/)
flask>=3.0

# AWS (upload S3, producer Kinesis)
boto3>=1.34

# Notebooks
jupyter>=1.0

# Testes
pytest>=8.0
